# Добавим репозиторий с бэкпортами
echo "deb http://deb.debian.org/debian buster-backports main" | sudo tee -a /etc/apt/sources.list

# Обновим и установим
sudo apt update
sudo apt install -t buster-backports tree
